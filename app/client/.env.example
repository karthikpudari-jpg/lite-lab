# Only needed when this frontend is deployed separately from the API server
# (e.g. this client on Netlify, API hosted elsewhere). Leave unset for the
# normal single-URL deployment where the API server also serves this build.
VITE_API_BASE_URL=
